{
  "name": "study-token-studio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "split-tokens": "node scripts/split-tokens.mjs",
    "build:tz:light": "THEME=light npx tz build",
    "build:tz:dark": "THEME=dark npx tz build",
    "build:tz": "npm run split-tokens && npm run build:tz:light && npm run build:tz:dark",
    "build:sd": "npx style-dictionary build --config style-dictionary.config.mjs",
    "build": "npm run build:tz && npm run build:sd",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nagayama/study-token-studio.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/nagayama/study-token-studio/issues"
  },
  "homepage": "https://github.com/nagayama/study-token-studio#readme",
  "devDependencies": {
    "@terrazzo/cli": "^0.10.5",
    "@terrazzo/plugin-css": "^0.10.4",
    "@terrazzo/plugin-tailwind": "^0.3.3",
    "style-dictionary": "^5.3.3"
  }
}
